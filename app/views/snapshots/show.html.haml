- model_class = Snapshot
.page-header
  %h1=t '.title', :default => model_class.model_name.human.titleize

%p
  %strong= model_class.human_attribute_name(:created_at) + ':'
  %br
  = @snapshot.created_at

= image_tag @snapshot.shot.url